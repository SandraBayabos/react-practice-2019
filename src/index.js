import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// So App.js is being rendered into this element called 'root'
ReactDOM.render(<App />, document.getElementById("root"));
